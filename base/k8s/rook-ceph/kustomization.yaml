namespace: rook-ceph
resources:
  - crds.yaml
  - common.yaml
  - operator.yaml
  - cluster.yaml

patches:
  - patch: |-
      - op: add
        path: /metadata/labels/pod-security.kubernetes.io/audit
        value: privileged
    target:
      kind: Namespace