resources:
  - home-assistant-secrets.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

configMapGenerator:
  - files:
    - configuration.yaml
    name: home-assistant-config
generatorOptions:
  disableNameSuffixHash: true