helmCharts:
- name: grafana.yaml
  includeCRDs: true
  valuesInLine:
    sidecar:
      datasources:
        enabled: true
        label: grafana_datasource
  repo: https://grafana.github.io/helm-charts